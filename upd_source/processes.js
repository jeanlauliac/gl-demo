/* @flow */

'use strict';

import {Event} from './event';

/**
 * Update the list of asynchronous processes currently running when a process
 * just completed, or 
 */
export function update(
  processes: Processes,
  event: Event,
  graph: Graph,
): Processes {
  
}
